# Subindo um container com projeto golang

- Realizar o build do container

```$ docker build --tag docker-gs-ping```

- Verificar se se imagem fio criada com sucesso

```$ docker image ls```

- Rodar seu container

```$ docker run codeeducation```